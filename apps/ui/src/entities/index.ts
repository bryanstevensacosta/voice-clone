/**
 * Entities Layer - Business entities
 *
 * This layer contains business entities that represent
 * core domain concepts. Each entity has:
 * - model/ - Types, state management (Zustand), validation
 * - api/ - Tauri commands for entity operations
 * - ui/ - Entity-specific UI components
 *
 * Entities are independent and can only depend on the shared layer.
 */

// Export entities as they are created
// Example:
// export * from './profile';
// export * from './sample';
// export * from './generation';
// export * from './tts-model';
